#!/usr/bin/env python

# Importing libraries
import subprocess
import os

# A function to list the files and
# the matched lines & their line numbers
def list_sightings(args): 
    
    # The grep command
    cmd = "grep"

    # folder sightings
    grep args

    # file sightings
